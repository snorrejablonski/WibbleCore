@model WibbleCore.ViewModels.MoviesViewModel
@{
    ViewData["Title"] = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>

<ul>
    @foreach (var movie in Model.Movies)
    {
    <li>@Html.ActionLink(movie.Name, "Details", "Movies", new { movie.Id})</li>
    }
</ul>